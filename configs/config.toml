[default]
input_directory = "input/"

# Grid settings
[default.grid]
cell_size = 300  # meters
direction = 8  # number of neighbors (4, 6, or 8)

# File paths
[default.files]
urban_beats_file = "Fehraltorf_Squares_300m_Blocks.dbf"
calibration_file = "s0param.csv"
alternative_water_file = "alternative0.csv"
groundwater_file = "gw0m_IDW2Sqr300fromswbi.csv"
soil_file = "in_soilparameter_new.csv"
et_file = "in_etparameter.csv"
climate_file = "forcing.csv"
open_water_file = "forcingOW.csv"
reuse_settings_file = "settingreuse.csv"
demand_file = "profile_percentage.csv"

# Simulation settings
[default.simulation]
year = 2018
time_step = 1  # days

# Output settings
[default.output]
output_directory = "results"
detailed_results = true

# Plotting settings
[default.plot]
modules = ["roof", "groundwater", "vadose"]
variables = ["storage", "evaporation", "water_level", "moisture"]
cells = [43, 120]  # List of cell IDs to plot
aggregated_variables = ["runoff", "evapotranspiration", "imported_water"]
